apiVersion: v1
kind: Service
metadata:
  name: besu-rpc-service
  namespace: {{ .Values.global.namespaces.besu }}
spec:
  type: ClusterIP
  selector:
    app: besu-rpc # Solo apunta a los nodos de lectura
  ports:
  - name: rpc
    port: 8545
    targetPort: rpc