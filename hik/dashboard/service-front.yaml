apiVersion: v1
kind: Service
metadata:
  name: hik-dashboard-frontend-svc
  namespace: hik
spec:
  type: LoadBalancer # O NodePort si estás en una red interna
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: dashboard-frontend