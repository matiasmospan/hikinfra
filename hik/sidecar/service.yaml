apiVersion: v1
kind: Service
metadata:
  name: hik-svc
  namespace: hik
spec:
  type: LoadBalancer
  selector:
    app: hik-sidecar
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080